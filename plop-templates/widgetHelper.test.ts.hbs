import { get{{pascalCase name}}SafeData } from './{{camelCase name}}Helper'; // Please use mocked
data from src/helpers/tests/mockedData/ const mockedData = { currentTime: '' } describe('get{{pascalCase
  name
}}SafeData', () => { it('should return correct data', () => { const result = get{{pascalCase
  name
}}SafeData(mockedData); expect(result).toEqual(' TODO '); }) })