import {
{{pascalCase name}}Type } from '../../{{pascalCase name}}Types'; export type
{{pascalCase name}}ChartType = { data:
{{pascalCase name}}Type | null; isSmallVersion?: boolean; }; const
{{pascalCase name}}Chart = ({ data, isSmallVersion }:
{{pascalCase name}}ChartType) => { return
<div>{{pascalCase name}}</div>; }; export default
{{pascalCase name}}Chart;