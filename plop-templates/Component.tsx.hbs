import React from 'react';

export type {{pascalCase name}}PropsType = Record<string, never>;

const {{pascalCase name}} = ({}: {{pascalCase name}}PropsType): JSX.Element => {
  return <div data-testid="{{snakeCase name}}_{{uniqueId}}">{{pascalCase name}}</div>;
};

export default {{pascalCase name}};
