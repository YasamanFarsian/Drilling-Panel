export type WidgetStyleType = {
  status: {
    backgroundColor: string;
    textColor: string;
  };
  noConnection: {
    containerBorder: string;
  };
  dropdown: {
    borderColor: string;
    dimension: number;
  };
  settings: {
    dimension: number;
  };
};
