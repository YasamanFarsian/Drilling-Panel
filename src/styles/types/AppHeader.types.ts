export type AppHeaderStyleType = {
  toggleButton: {
    themeToggleButton: {
      backgroundColor: string;
      activeIcon: string;
      width: string;
      height: string;
      iconWidth: string;
      iconHeight: string;
    };
  };
};
